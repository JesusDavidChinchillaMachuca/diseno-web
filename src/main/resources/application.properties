spring.application.name=PaginaWeb

# Puerto del servidor
server.port=8080

# PostgreSQL en Render
spring.datasource.url=jdbc:postgresql://dpg-d1g1jqnfte5s7384e5dg-a.oregon-postgres.render.com:5432/db_diseno_web
spring.datasource.username=admin_diseno
spring.datasource.password=RWCdG1sLPrOpeRhqWwyJ6YLM6D64OJOb

# Driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA (Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Codificación
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# Si decides separar el frontend durante desarrollo
# Cors general (mejor configurarlo por controlador en producción)
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Carpeta de recursos estáticos (para frontend compilado)
spring.web.resources.static-locations=classpath:/static/

# Ruta por defecto para index.html si vas a servir React embebido
spring.mvc.view.prefix=/
spring.mvc.view.suffix=.html
